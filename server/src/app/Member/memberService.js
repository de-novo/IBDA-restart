import { errResponse, response } from "../../../config/response.js";
import baseRsponseStatus from "../../../config/baseRsponseStatus.js";
import { pool } from "../../../config/database.js";

export const functionName = async function (...args) {
    try {
    } catch (err) {
    } finally {
    }
};
